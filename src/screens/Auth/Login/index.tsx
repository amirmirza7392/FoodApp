import {StyleSheet, View} from 'react-native';
import React from 'react';
import CustomText from '../../../components/CustomText';

const Login = () => {
  return (
    <View>
      <CustomText label="Login" />
    </View>
  );
};

export default Login;

const styles = StyleSheet.create({});
